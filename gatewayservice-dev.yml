#Common propeties

server:
  port: 8761

spring:
 application:
   name: EUREKA-DISCOVERY-SERVER
eureka:
 client:
   register-with-eureka: false
   fetch-registry: false

management:
 endpoints:
   web:
     exposure:
       include: '*'
